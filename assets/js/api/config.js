// Demo stand-in for the real config.js. The backend is deliberately
// unreachable, so both chat widgets take their signed-out path and run in
// local preview mode against localStorage.
export const javaURI = 'http://127.0.0.1:9';
export const pythonURI = 'http://127.0.0.1:9';
export const fetchOptions = { credentials: 'include', headers: {} };
